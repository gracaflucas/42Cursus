Libft is about creating your own library in C, with a handful of functions to help you manipulate strings and linked lists.
How to use:
With the library inside your folder, the only thing needed is to "make"
After that, compile your file with the library, for example: "cc your_archive libft.a"
